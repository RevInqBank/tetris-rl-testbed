"""LEARN 화면에 그대로 실리는 한국어 문안 — "여기서 크리틱은 무엇을 하는가".

이 파일이 그 문단의 확정본이다. `web` 은 번역하지 않고 `meta.critic_role_ko` 를
그대로 띄운다. 표현은 사용자의 `RL_공부계획.pdf` 용어에 맞췄다 — "기준선",
"부트스트랩 표적", "액터가 사라진 극단", "탐색이 정책 개선을 한다" 같은 말이
그 문서에서 온 것이라 임의로 바꾸면 공부하던 축과 어긋난다.

각 항목은 세 조각이다.
    headline : 한 줄. 패널 제목 옆에 붙는다
    body     : 2~4문장. 화면의 본문
    formula  : 갱신식. 유니코드 텍스트, LaTeX 아님
"""

CRITIC_ROLES_KO = {
    "random": {
        "headline": "크리틱 없음 — 대조군",
        "body": "합법 배치 중 하나를 균등하게 고른다. 학습도 평가함수도 없다. "
                "나머지 전략들이 무엇을 이겨내고 있는지를 보여주는 바닥선이다.",
        "formula": "π(a|s) = 1 / |합법 배치|",
    },
    "dellacherie": {
        "headline": "크리틱 없음 — 사람이 손으로 정한 평가함수",
        "body": "학습하지 않는다. 사람이 특징 6개의 가중치를 직접 정해 놓은 것을 "
                "그대로 쓴다. 여기서 '평가'를 하는 주체는 알고리즘이 아니라 사람이다. "
                "학습된 정책들이 이 손계산 기준선을 넘는지가 관전 포인트다.",
        "formula": "score(a) = w_사람 · f(afterstate),   a* = argmax",
    },
    "cem_linear": {
        "headline": "크리틱 없음 — 진화전략이 가중치를 직접 탐색한다",
        "body": "가치함수도, 경사도, 시간차(TD)도 없다. 게임 전체를 "
                "'가중치 8개를 넣으면 점수 하나가 나오는 블랙박스'로 보고 "
                "가중치 공간을 직접 뒤진다. 어느 수가 좋았는지 따지는 크레딧 할당을 "
                "아예 하지 않는다는 뜻이다.\n"
                "공부계획의 축 밖에 있지만, 축 위의 네 알고리즘을 크게 이긴다. "
                "정책이 숫자 8개뿐이고 롤아웃이 싸면 크레딧 할당 기계장치가 "
                "값을 못 한다는 뜻이다 — 크리틱은 정책이 크고 표본이 비쌀 때 값을 한다.",
        "formula": "N(μ, σ²) 에서 w 를 뽑아 → 게임을 돌려 점수를 재고 → "
                   "상위 elite 의 평균·분산으로 μ, σ 갱신",
    },
    "reinforce": {
        "headline": "크리틱 없음 · 실제 리턴 G_t 만 쓴다",
        "body": "v̂ 라는 함수 자체가 존재하지 않는다. 갱신의 크기를 정하는 것은 "
                "에피소드가 끝난 뒤 실제로 세어 본 리턴이다.\n"
                "편향이 전혀 없는 대신 분산이 크다. 그 에피소드에서 일어난 모든 우연 — "
                "어떤 조각이 나왔는지, 언제 죽었는지 — 이 리턴 하나에 통째로 섞여 들어와 "
                "그대로 파라미터에 곱해진다. 게다가 G_t 는 끝까지 가 봐야 알 수 있으므로 "
                "에피소드가 끝나기 전에는 한 발짝도 배울 수 없다.",
        "formula": "θ ← θ + α · G_t · ∇ln π(A_t | S_t)",
    },
    "reinforce_baseline": {
        "headline": "분산 감소 기준선 — 부트스트랩은 하지 않는다",
        "body": "v̂ 가 등장하지만 하는 일은 분산을 깎는 것뿐이고, 갱신이 가리키는 "
                "방향에는 손대지 않는다. 행동에 의존하지 않는 어떤 값을 빼도 "
                "기댓값에 흔적이 남지 않기 때문이다 — 확률의 합은 언제나 1이고 "
                "상수의 기울기는 0이라, Σ_a b(s)∇π(a|s) = b(s)∇1 = 0 이다.\n"
                "그래서 v̂ 가 엉터리여도 갱신 방향은 평균적으로 옳다. "
                "어느 행동이 좋았는지 판정하지 않으므로 크리틱이라 불릴 자격이 없고, "
                "Sutton 이 이것을 액터-크리틱으로 분류하지 않는 이유가 정확히 이것이다.",
        "formula": "θ ← θ + α ( G_t − v̂(S_t) ) ∇ln π(A_t | S_t)\n"
                   "w ← w + α_w ( G_t − v̂(S_t) ) ∇v̂(S_t)",
    },
    "a2c": {
        "headline": "부트스트랩 표적 — 여기서부터 크리틱이 값을 매긴다",
        "body": "②와 다른 곳은 딱 한 군데다. G_t 가 R + γ v̂(S_{t+1}) 로 바뀌었다. "
                "코드에서도 정말 한 줄이다 (rl/pg.py 의 THE ONE LINE).\n"
                "v̂(S_{t+1}) 은 지금 갱신하는 상태가 아니라 다음 상태의 추정값이다. "
                "이게 표적 안으로 들어가면 성질이 뒤집힌다 — v̂ 가 틀린 만큼 표적이 틀리고, "
                "표적이 틀리면 갱신 방향 자체가 틀린다. 편향이 생긴다. "
                "②에서는 v̂ 가 아무리 엉터리여도 방향이 안 변했는데 ③에서는 변한다.\n"
                "대가로 얻는 것은 에피소드가 끝나기를 기다리지 않아도 된다는 것이다. "
                "한 스텝만 밟으면 표적이 계산되므로 온라인으로, 매 스텝 배운다.\n"
                "→ 크리틱의 유무가 아니라 부트스트랩 참여 여부가 분류를 가른다.",
        "formula": "θ ← θ + α ( R + γ v̂(S_{t+1}) − v̂(S_t) ) ∇ln π(A_t | S_t)\n"
                   "w ← w + α_w ( R + γ v̂(S_{t+1}) − v̂(S_t) ) ∇v̂(S_t)",
    },
    "dqn": {
        "headline": "Q 가 정책 그 자체 — 액터가 사라진 극단",
        "body": "액터가 없어진 게 아니라 파라미터가 0개인 액터가 된 것이다. "
                "argmax_a Q 가 액터이고, 크리틱이 정해지는 순간 정책이 딸려 나온다. "
                "③에서 π(a|s) 를 지우고 a = argmax_a Q 로 대체한 것이 ④다.\n"
                "이 종속의 대가가 on/off-policy 의 정체다. 표적을 보면 행동 정책이 "
                "어디에도 나오지 않는다 — (s,a,r,s') 는 환경이 준 표본이고 max 는 누가 "
                "그 행동을 골랐는지 상관하지 않는다. 그래서 낡은 정책이 만든 데이터도 "
                "여전히 유효하고, 리플레이 버퍼가 성립하는 근거가 이 한 줄이다. "
                "정책 그래디언트는 상태분포와 q 가 둘 다 지금 정책에 딸려 있어서 반대다. "
                "on/off-policy 는 설계자의 취향이 아니라 식에서 강제되는 성질이다.\n"
                "afterstate 를 쓰므로 max_a′ 는 다음 조각의 배치를 열거하는 루프로 "
                "자연히 풀린다. 행동마다 출력 헤드를 둘 필요가 없다.",
        "formula": "y = r + γ max_a′ Q(s′, a′)      ← 여기에 행동 정책이 없다\n"
                   "L = ( Q(s,a) − y )²,    π(s) = argmax_a Q(s,a)",
    },
    "search_1ply": {
        "headline": "탐색이 정책 개선을 하고, 가치는 말단만 평가한다",
        "body": "가중치는 CEM 이 학습한 것을 그대로 쓴다. 재학습이 없으므로 "
                "패널 3과의 차이는 전부 탐색 덕분이다.\n"
                "크리틱은 정책을 만들지도, 미분당하지도 않는다. 정책 개선을 하는 것은 "
                "탐색이고 가치함수는 말단 국면에 점수를 매길 뿐이다. "
                "앞의 여섯과 층이 다른 지점이 여기다 — 정책 개선 연산자가 신경망 바깥에 있다. "
                "공부계획 4주차 AlphaZero 절의 구조적 주장만 남기고 MCTS·정책 헤드·"
                "자기대국은 걷어낸 최소 형태다.\n"
                "미리보기가 없는 난이도(익스트림)에서는 볼 미래가 없으므로 "
                "0-ply 로 퇴화해 CEM 과 같은 정책이 된다. 그 붕괴를 보이는 것이 "
                "'미리 돌려볼 모델이 있나'라는 물음의 실물 답이다.",
        "formula": "score(p₁) = max over p₂ of  w · f(p₁ → p₂ 이후 국면)",
    },
    "search_1ply_sum": {
        "headline": "폐기된 탐색 규칙 — 기록용으로만 남긴다",
        "body": "깊이가 다른 두 국면의 가치를 더하던 옛 규칙이다. 중간 노드를 두 번 세고, "
                "landing_height·eroded_piece_cells 를 서로 다른 두 배치에 대해 더한다 — "
                "각각 자기 수의 속성이라 더하는 의미가 없다.\n"
                "실측으로 이 규칙은 탐색 없는 CEM 보다 점수가 낮았고, 미리보기를 없애 "
                "탐색을 붕괴시키면 오히려 점수가 올랐다. '탐색을 껐더니 좋아졌다'는 "
                "탐색이 도움이 안 된다는 뜻이 아니라 평가 규칙이 틀렸다는 신호다. "
                "무엇을 왜 고쳤는지 남기려고 보존한다.",
        "formula": "score(p₁) = w · f(p₁ 이후) + max over p₂ of w · f(p₁ → p₂ 이후)   ← 폐기",
    },
    "cem_search": {
        "headline": "탐색을 넣고 학습한 가치 — 패널 8의 교란 요인을 제거한 것",
        "body": "패널 8은 CEM이 **1스텝 그리디용으로** 맞춘 가중치를 2수 탐색에 갖다 쓴 것이라, "
                "가치함수가 자기가 쓰일 깊이에서 좋도록 학습된 적이 없었다. 그래서 탐색이 "
                "오히려 손해였다.\n"
                "여기서는 롤아웃 자체가 2수 탐색으로 수를 고른다. 즉 CEM이 "
                "**탐색 정책의 가치함수를** 찾는다. 탐색 이득을 말하려면 이 조건이어야 한다.\n"
                "대가는 비용이다 — 배치마다 약 25배 느려서 같은 예산에 훨씬 적은 게임밖에 못 본다. "
                "AlphaZero가 자기대국에 막대한 계산을 쓰는 이유가 이것이다: "
                "탐색이 정책 개선을 하려면 그 탐색을 학습 루프 안에 넣어야 하고, 그 순간 비싸진다.",
        "formula": "롤아웃의 수 선택 = argmax_p₁ max_p₂ v(p₁→p₂ 이후)   (그리디가 아니다)",
    },
    "cem_score_safe": {
        "headline": "적합도에 생존을 곱했다 — 패널 12와 목적함수만 다르다",
        "body": "패널 12는 **조각당 점수만** 봤다. 그래서 일찍 죽어도 죽기 전 점수가 "
                "높으면 좋은 후보로 뽑혔다 — 실제로 점수 1위인데 상한 50,000조각에서 "
                "**10판 전부 죽었다**(중앙값 1,325줄). "
                "**생존이 점수 계산을 멈추기는 하지만 평균을 멈추지는 않는다.**\n"
                "여기서는 적합도가 `조각당 점수 × min(1, 생존조각/조각상한)`이다. "
                "일찍 죽으면 그만큼 비례해 깎이고, 상한까지 가면 점수가 그대로 남는다. "
                "**고득점하고 일찍 죽는 후보가 비슷한 점수로 오래 사는 후보 아래로 밀린다.**\n"
                "곱셈을 택한 이유는 **임의 상수가 없다는 것**이다. 임계값도, 벌점 계수도 "
                "고를 필요가 없고 모든 수가 그 실행 자체에서 나온다. "
                "(상한 생존만 세는 방식과 벌점을 빼는 방식도 구현해 뒀지만, 앞은 초반에 "
                "학습 신호가 사라지고 뒤는 계수를 손으로 골라야 한다.)\n"
                "**결과**(상한 50,000조각, 같은 시드 10판, 엔진 기준): 중앙값 **19,992줄**, "
                "10판 중 **7판이 상한까지** 갔다. 패널 12의 중앙값 1,325줄에서 15배다. "
                "대가는 점수다 — 정규화 점수/조각 82.4 → 74.8, 4줄 삭제 비율 58.5% → 44.5%. "
                "**점수를 9% 내주고 생존을 15배 얻은 교환이다.**",
        "formula": "적합도 = 평균over조각(score_delta / level) × min(1, 생존조각 / 조각상한)",
    },
    "cem_score_wells": {
        "headline": "특징 두 개를 더했더니 전략이 열렸다 — 표현력 문제였다",
        "body": "패널 11과 **같은 55분, 같은 목표, 같은 알고리즘**이다. "
                "특징 두 개(`max_well_depth`, `well_count`)만 더했다.\n"
                "그러자 tetris 비율이 38.9% → 59.2%로, 조각당 점수가 61.8 → 82.2로 올랐다.\n"
                "**생존은 개선되지 않았다.** 상한 3,000조각으로 재면 '10판 전부 죽던 것이 "
                "6판은 상한까지'로 보였지만, 상한 50,000조각에서는 **양쪽 다 0/10**이고 "
                "사망 시점도 거의 같다. 상한 3,000이 하필 사망 분포의 한가운데였다. "
                "**여기서 바뀐 것은 점수이지 생존이 아니다** — 생존은 패널 16이 따로 바꾼다.\n"
                "학습된 가중치가 전략을 그대로 쓴다 — `max_well_depth +0.575`, "
                "`well_count −0.350`. **깊은 우물을 정확히 하나 유지해라.** "
                "아무도 그 부호를 지정하지 않았다.\n"
                "왜 8종으로는 안 됐나: `cumulative_wells`는 우물의 **비용**을 재는 항이라 "
                "'깊은 우물 하나(자산)'와 '얕은 홈 여럿(부채)'을 구분하지 못한다 — "
                "같은 총량이 나오기 때문이다. **선형 정책이 표현할 수 없는 전략은 "
                "탐색을 아무리 오래 돌려도 찾지 못한다.**",
        "formula": "적합도는 패널 11과 동일. 특징만 8개 → 10개 "
                   "(+max_well_depth, +well_count)",
    },
    "cem_score": {
        "headline": "크리틱 없음 — 알고리즘은 그대로, 목표만 바꿨다",
        "body": "패널 3과 완전히 같은 CEM, 같은 특징 8개, 같은 학습 예산이다. "
                "적합도만 '지운 줄 수'에서 '레벨 보정 조각당 점수'로 바꿨다.\n"
                "그러자 성향이 뒤집혔다. 패널 3은 영원히 한 줄씩 깎으며 죽지 않고, "
                "이쪽은 우물을 열어둔 채 I 조각을 기다렸다가 한 번에 4줄을 터뜨린다. "
                "대신 기다리는 동안 높이가 올라가 자주 죽는다.\n"
                "학습된 가중치에서 bumpiness 의 부호가 뒤집힌 것(−0.139 → +0.217)이 "
                "그 성향이다 — 표면을 일부러 울퉁불퉁하게, 즉 한 열을 비워 둔다는 뜻이고 "
                "아무도 가르치지 않았는데 그렇게 배치됐다.\n"
                "무엇을 최적화하라고 말하느냐가 알고리즘 선택만큼 결과를 바꾼다.",
        "formula": "적합도 = 평균over조각( score_delta / level )     (줄 수가 아니다)",
    },
}


# 왜 레벨로 나누는지 — UI 각주로 쓸 수 있게 따로 뺀다.
SCORE_METRIC_NOTE_KO = (
    "조각당 점수는 반드시 레벨로 나눠야 한다. 레벨 = 1 + 지운줄/10 이 게임 내내 오르며 "
    "모든 삭제 점수에 곱해지므로, 나누지 않으면 레벨 60에서의 싱글(6,000점)이 "
    "레벨 1에서의 테트리스(800점)보다 커진다. 즉 그 지표는 '얼마나 오래 살았나'를 "
    "다시 재게 된다. 실제로 보정 전 적합도로 학습했을 때 조각당 1,546점인데 "
    "테트리스 비율 0.1%인 정책 — 오래 살며 싱글만 긁는 정책 — 이 나왔다."
)

CAP_NOTE_KO = (
    "조각 상한에 걸린 게임은 끝난 게임이 아니다. 그 줄 수는 실력의 측정치가 아니라 "
    "평가 상한이 정한 하한이다. 상한 도달이 10/10 인 전략끼리는 줄 수로 순위를 매길 수 없다."
)

MEDIAN_NOTE_KO = (
    "주 통계는 중앙값이다. 이 분포들은 꼬리가 두꺼워서 한 판의 운이 평균의 절반을 "
    "만들기도 한다 — DQN 의 평균 103.7줄 중 53%가 한 판(547줄)에서 나왔고 중앙값은 40이다. "
    "평균으로는 DQN 이 A2C 보다 위지만 중앙값으로는 뒤집힌다. 평균과 중앙값이 크게 "
    "벌어진다는 사실 자체가 결과이므로 둘 다 표시한다."
)


if __name__ == "__main__":
    import json
    print(json.dumps({"critic_roles_ko": CRITIC_ROLES_KO,
                      "score_metric_note_ko": SCORE_METRIC_NOTE_KO,
                      "cap_note_ko": CAP_NOTE_KO,
                      "median_note_ko": MEDIAN_NOTE_KO},
                     ensure_ascii=False, indent=1))
